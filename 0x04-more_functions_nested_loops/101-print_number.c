#include "main.h"

/**
 * print_number - prints an integer
 * @n:.input integer parameter
 */

void print_number(int n)
{
	unsigned int n;

	scanf("%d", &n);

	if (n > 0)
		_putchar(n);
	if (n = 0)
		_putchar(n);
	else
		_putchar(n);

	return 0;	
}
