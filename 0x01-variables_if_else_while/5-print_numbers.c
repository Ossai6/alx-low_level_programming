#include <stdio.h>

/**
 * main - A program to print single base 10 digits
 * Return: Always return 0
 */
int main(void)
{
	int i;

	while (i < 10)
	{
		printf("%d", i);
	i++;
	}
	printf("\n");
	return (0);
}
