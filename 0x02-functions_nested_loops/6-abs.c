#include "main.h"

/**
 * _abs - Prints the absolute value of a number
 * @n: Value to be computed
 * Return: Return the abs of the int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-1 * n);
}
