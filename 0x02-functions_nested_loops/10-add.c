#include "main.h"

/**
 * add - Adds two numbers
 * @a: Integer value
 * @b: Integer value
 * Return: Resume the sum
 */

int add(int a, int b)
{
	int sum = 0;

	sum = a + b;
	_putchar(sum);

	return (sum);
}
