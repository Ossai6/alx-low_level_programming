#include "main.h"

/**
 * print_sign - Prints sign if greater, less or equal 0
 * @n: Char to be checked
 * Return: Return 1 if greater than 0, 0 if equal to 0, -1 if less than 0
 */

int print_sign(int n)
{
	if (n > 0)
		_putchar('+');
	return (1);

	if (n == 0)
		_putchar('0');
	return (0);

	if else
		_putchar('-');
	return (-1);
}
